<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cuc.constellationapp">
    <!-- 添加网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/star_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/star_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".view.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activity.CommunityActivity"></activity>
        <activity android:name=".view.activity.WriteActivity"></activity>
        <activity android:name=".view.activity.TarotActivity"></activity>
        <activity android:name=".view.activity.TestActivity"></activity>
        <activity android:name=".view.activity.LoginActivity"></activity>
    </application>

</manifest>